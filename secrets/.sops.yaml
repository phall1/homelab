keys:
  # Replace with YOUR age public key (from: age-keygen -y ~/.config/sops/age/keys.txt)
  - &admin age1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

creation_rules:
  - path_regex: .*\.yaml$
    key_groups:
      - age:
        - *admin
